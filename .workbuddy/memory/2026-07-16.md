## 2026-07-16

### Vulhub-Web 项目重构 & 推送到 GitHub
- 将项目从扁平结构重构为 `app/` Python 包结构
- 修正所有导入路径：`from xxx` → `from app.xxx`
- 修复 Windows 兼容性：`pty`/`fcntl` 改为条件导入（仅 WebSocket 终端函数内部）
- 新增开源项目标准文件：
  - `README.md` — 英文完整文档（架构图、API 清单、部署说明）
  - `LICENSE` — MIT
  - `CONTRIBUTING.md` — 开发指南
  - `CHANGELOG.md`
  - `.gitignore` — 排除 `data/*.db`、`__pycache__/`、`.env`
  - `.env.example`
  - `Dockerfile` + `docker-compose.yml`
  - `run.py` — 项目入口
- Git init → 首次提交 (37 files) → 推送到 GitHub
- 仓库地址：https://github.com/Fuzzy-World/vulhub-web
- GitHub 账号：Fuzzy-World
- 验证通过：`from app.main import app` 正常加载 35 条路由

### Docker 部署结构修正
- 用户反馈：Vulhub-Web 镜像不应包含 Vulhub 本体（4GB+）
- 修正 Dockerfile：移除 `docker.io`/`docker-compose` 内部安装，改用 volume 挂载宿主机 docker.sock
- 修正 docker-compose.yml：挂载 `../vulhub` 兄弟目录 → `/vulhub:ro`
- 理想结构：`~/work/{vulhub/ + Vulhub-Web/}` 同级
- README 更新部署说明，明确镜像内只有管理平台
- 已推送（commit c0df95e + 12f9629）
